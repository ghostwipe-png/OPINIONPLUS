# Copy to .dev.vars for local `wrangler dev`. Never commit real secrets.
SESSION_SECRET=change-me-to-a-long-random-string
ADMIN_PIN_HASH=03ac674216f3e15c761ee1a5e255f067953623c8b388b4459e13f978d7c846f4
# ^ this is the sha-256 hash of "1234" — the same demo PIN used in the
# frontend's admin panel mock. Replace with your own PIN's hash before
# going live: echo -n "yourpin" | shasum -a 256
