{
  "name": "opinionplus-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute opinionplus --local --file=./migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute opinionplus --remote --file=./migrations/0001_init.sql"
  },
  "dependencies": {
    "fast-xml-parser": "^5.10.1",
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
